<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Style x:Key="Badge.Base" TargetType="Border">
    <Setter Property="CornerRadius" Value="6"/>
    <Setter Property="Padding" Value="10,4"/>
    <Setter Property="HorizontalAlignment" Value="Left"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
  </Style>

  <Style x:Key="Badge.Success" TargetType="Border" BasedOn="{StaticResource Badge.Base}">
    <Setter Property="Background" Value="#1A47D18C"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Success}"/>
    <Setter Property="BorderThickness" Value="1"/>
  </Style>

  <Style x:Key="Badge.Warning" TargetType="Border" BasedOn="{StaticResource Badge.Base}">
    <Setter Property="Background" Value="#1AF4B942"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Warning}"/>
    <Setter Property="BorderThickness" Value="1"/>
  </Style>

  <Style x:Key="Badge.Critical" TargetType="Border" BasedOn="{StaticResource Badge.Base}">
    <Setter Property="Background" Value="#1AFF4D5E"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Critical}"/>
    <Setter Property="BorderThickness" Value="1"/>
  </Style>

  <Style x:Key="Badge.Info" TargetType="Border" BasedOn="{StaticResource Badge.Base}">
    <Setter Property="Background" Value="#1A62A8FF"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Information}"/>
    <Setter Property="BorderThickness" Value="1"/>
  </Style>

  <Style x:Key="Badge.Neutral" TargetType="Border" BasedOn="{StaticResource Badge.Base}">
    <Setter Property="Background" Value="{DynamicResource Brush.Elevated}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}"/>
    <Setter Property="BorderThickness" Value="1"/>
  </Style>

  <Style x:Key="Badge.Mode" TargetType="Border" BasedOn="{StaticResource Badge.Base}">
    <Setter Property="Background" Value="#22E2231A"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Accent}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="12,5"/>
  </Style>

  <Style x:Key="Badge.Text" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{DynamicResource Font.UI}"/>
    <Setter Property="FontSize" Value="{DynamicResource FontSize.Caption}"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
  </Style>

  <Style x:Key="Status.Dot" TargetType="Ellipse">
    <Setter Property="Width" Value="8"/>
    <Setter Property="Height" Value="8"/>
    <Setter Property="Margin" Value="0,0,8,0"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
  </Style>

</ResourceDictionary>
